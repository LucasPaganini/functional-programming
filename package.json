{
  "name": "lucas-paganini-fp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist/",
    "build": "npm run clean && tsc -p tsconfig.app.json",
    "test": "npm run clean && tsc -p tsconfig.spec.json && jasmine --config=jasmine.json",
    "sandbox": "npm run build && node dist/sandbox.js"
  },
  "author": "Lucas Paganini",
  "license": "MIT",
  "devDependencies": {
    "@types/jasmine": "^3.3.14",
    "@types/lodash": "^4.14.136",
    "@types/node": "^12.6.8",
    "fp-ts": "^2.0.2",
    "jasmine": "^3.4.0",
    "jasmine-spec-reporter": "^4.2.1",
    "rimraf": "^2.6.3",
    "typescript": "^3.5.3"
  }
}
